'use strict';

(function (app) {})(angular.module('app', ['angular-ui-router', 'app.views', 'app.bidule', 'app.services', 'app.config']));
;'use strict';

(function (app) {
    'use strict';
})(angular.module('app.bidule', []));
;'use strict';

(function (app) {})(angular.module('app.config', []));
;'use strict';

(function (app) {})(angular.module('app.services', []));
;'use strict';

(function (app) {
    'use strict';

    app.component("bidule", {
        templateUrl: 'js/components/bidule/bidule.html'
    });
})(angular.module('app.bidule'));
;'use strict';

(function (app) {
    'use strict';

    app.config(['$stateProvider', '$urlRouterProvider', '$locationProvider', function ($stateProvider, $urlRouterProvider, $locationProvider) {
        $locationProvider.hashPrefix('!');
        $urlRouterProvider.otherwise('/');
        $stateProvider.state('app', {
            url: '',
            abstract: true,
            template: '<ui-view></ui-view>'
        });
        // .state('app.bidule', {
        //     url: '/',
        //     template: '<bidule></bidule>'
        // })
    }]);
})(angular.module('app.config'));
;'use strict';

(function (app) {

    'use strict';

    app.service('bidulesService', function ($http) {
        return {
            get: function get() {
                return $http.get('/api/bidules');
            },
            add: function add(bidule) {
                return $http.post('/api/bidules', bidule);
            },
            edit: function edit(bidule) {
                return $http.put('/api/bidules/' + bidule._id, bidule);
            },
            delete: function _delete(bidule) {
                return $http.delete('/api/bidules/' + bidule._id);
            }
        };
    });
})(angular.module('app.services'));
;
//# sourceMappingURL=app.min.js.map